# GuideForge 换机器交接文档(2026-09-08)

> 使用场景:把本项目的全部工作状态迁到另一台机器继续干。
> 本项目权威副本 = gitee 仓库(代码+数据产物全部已推),大文件不入 git(见 §4)。
> 本机工作目录:`C:\Users\zhangdanjing\guideforge`(WSL 路径 `/mnt/c/Users/zhangdanjing/guideforge`)

## 1. 这是什么项目(一句话)

赛道B(全球大学生生命科学挑战赛·AI 驱动的基因编辑与功能核酸工具设计):
**Cas12a2 crRNA 骨架分型设计与 AI 智能体**——固定 spacer、AI 优化 crRNA 骨架(DR 茎环),
主张"骨架最优解随靶标(spacer 上下文)变化,应按靶分型选骨架",交付四层 AI 管线
(蛋白预测/生成+BO/虚拟细胞/智能体)+ Web 演示,湿实验闭环(导师审定 v4 只做细胞杀伤)。

- gitee:https://gitee.com/eastern-zhongyuan/guideforge(分支 main)
- 当前已推送提交:`e03251d`(2026-09-08 同源跨上下文交互检验 §E)+ `103aa2f`(09-07~08 批量:APC Q1328x 定稿/湿实验 v4/分型稳健性等)
- 策划案(纸质/微信):mxl-Cas12a2_crRNA骨架分型与AI智能体设计方案V3.docx(老师评审口径)
- 湿实验执行单:`docs/wetlab_plan.md`(v4,2026-09-07/08,导师审定)

## 2. 换机器三步走

### 第 1 步:装主环境(python ≥3.10,纯 CPU 即可)

```bash
# Linux/WSL 推荐 miniconda
conda create -n guideforge python=3.11 -y
conda activate guideforge
conda install -c bioconda viennarna   # 或 pip install viennarna(import RNA)
pip install -i https://pypi.tuna.tsinghua.edu.cn/simple \
    numpy biopython scipy scikit-learn openpyxl matplotlib flask pytest
```

- 实测可用参照:`/home/dzy/赛道B/.venv`(有 RNA/scipy/numpy/openpyxl/sklearn;缺 pytest,契约测试用 `python -m unittest` 也可跑)
- 完整四引擎(逆折叠 Struct2SeQ / GRNADE / RNet-SHAPE)需要额外 engine 环境,
  **不是必须**——主管线纯 ViennaRNA CPU 可跑;engine 依赖见 `requirements-engine.txt`
- 可选 MD/Chai 环境:独立装(见 requirements.txt 尾部注释)

### 第 2 步:clone + 放回大文件

```bash
git clone https://gitee.com/eastern-zhongyuan/guideforge
```

> 推送凭据注意:WSL 里 Linux git 读不到 Windows 凭据,推送请用 Windows 侧 git:
> `"/mnt/c/Program Files/Git/cmd/git.exe" push origin HEAD`(或直接在 Windows 终端操作)

### 第 3 步:验证环境(必须全过才算交接成功)

```bash
cd guideforge
python -m pytest tests/ -q                    # 全量契约测试(或 python -m unittest discover -s tests)
python scripts/crrna_homolog_context_interaction.py   # §E 复算(需 §4 大文件)
python scripts/crrna_homolog_interaction_audit.py     # §E 独立审计 → AUDIT PASS
# README「一键复算清单」段:逐条可复算;跑通 crrna_scaffold_design.py 主管线即环境 OK
```

## 3. 当前状态速览(2026-09-08,全部已提交)

| 主题 | 状态 | 位置/口径 |
|---|---|---|
| 骨架分型主张 | **弱证据阳性**(计算)+ **同源原理层统计支持**(§E) | prereg §A + §E;README 头条 |
| 同源跨上下文检验(今天新增) | **E1 PASS** ρ=0.257[CI95 0.247,0.267](DeWeirdt 35,682 变体×2 上下文);E3b 位置1 碱基偏好 W=0.016(交互佐证);E3a 位置梯度 W=0.769(通用层) | `data/homolog_context_interaction.json` |
| 选型分类器 | 文献先验排序(16 对 LbCas12a 转录),非活性预测;fig1f 留出仅秩级 | `data/selector_model.json` |
| 四靶脱靶扫描 | GENCODE v47 全转录组,全部 exact 0mm=0 | `data/*_scan_v47.*` |
| 四模型 Sp8 判别 | 全 FAIL → out_of_model_domain(只限 Creutzburg 前体侧,Cas12a2 不受影响,8D4A 无假结互证) | README「状态竞争/NUPACK/Knotty」段 |
| 湿实验(导师 v4) | **只做实验三细胞杀伤**:TP53-R248Q(HCT116)+R273H(SW480)× 每靶 WT + A1G+U3A+A8G+U15C = **4 条**;实验一/二暂缓,10 条 panel 回补备用 | `docs/wetlab_plan.md`;下单表 `data/wetlab_panel_cell4_order.csv` / 备用 `wetlab_panel10_order.csv` |
| 靶点定稿 | APC = **Q1328\***(c.3982C>T,2026-09-08 拍板;旧 Q1312x/Q1338x 全部退役,仅 git 历史) | `data/agent/apc_q1328x.*` |
| 结构预测 | Chai-1 8D4A 自模板注入打通界面(ipTM 0.49,降级口径:模板合规性 sanity);AF3/Protenix 13 套输入云端手册 | `tools/protenix_cloud_runbook.md` + `docs/af3_server_runbook.md` |
| 智能体+Web | 输入突变→spacer+骨架型+预测输出;内置阴性退化机制(判据门控) | `scripts/crrna_design_agent.py` / `crrna_agent_webapp.py` |

### 本次 §E 涉及的文件(commit e03251d)
- `docs/preregistration.md` §E —— 判据登记(先登记后评估,铁律)
- `scripts/crrna_homolog_context_interaction.py` —— E1(DeWeirdt 跨方向 Spearman+CI)/E2(区位)/E3a/E3b(Tian 位置分层 Kendall W)
- `scripts/crrna_homolog_interaction_audit.py` —— 独立复算(不 import 被测模块)
- `tests/test_homolog_interaction.py` —— 契约测试 4 项
- `data/homolog_context_interaction.json` —— 结果(判读文字全部由数值按规则生成)
- `README.md` —— 证据链段落 + 一键复算清单

## 4. 不在 git 里的大文件(换机器必须单独处理!)

clone 后这些**不会自动出现**,缺了对应功能会挂:

| 文件 | 大小 | 干什么用 | 怎么拿 |
|---|---|---|---|
| `data/raw/gencode.v47.transcripts.fa` | 613 MB | 全转录组脱靶扫描(五靶 v47 重扫) | EBI GENCODE release_47 FTP 重下,或原机器拷 |
| `data/raw/deweirdt2020_dr_scan.json` | 17 MB | **§E 主判据 E1 的数据源**(不在 git!) | 原机器拷,或 `scripts/crrna_han2025_features.py` 从 `data/41587_2020_600_MOESM6_ESM.xlsx` 重新 parse |
| `data/raw/` 其余 MOESM xlsx/pdf | ~几十 MB | Han/Tian/DeWeirdt 数据链(在 data/ 顶层有 xlsx 副本) | 原机器拷 |
| toolbox 引擎权重 | 大 | Struct2SeQ/GRNADE/RNet 模型权重(可选) | 原机器拷,或按 toolbox/README 重下 |
| Chai-1 环境/权重 | 大 | 结构共折叠(可选,云端) | 独立装,见 README 安装段 |

> ⚠️ `deweirdt2020_dr_scan.json` 若缺失,`crrna_homolog_context_interaction.py` 直接失败
> (E1 无数据)。**换机器后第一件事就是把它放回 `data/raw/`。**

## 5. 未完成 / 下一步(按优先级)

1. **湿实验下单**:v4 cell4 的 4 条 DNA 模板(62nt,HPLC)下单;敲定 Cas12a2 蛋白来源(最长杆);确认/订购 HCT116、SW480、WT TP53 对照株(库存优先)。倒排 D-4 周起,提交窗口 10-01~08
2. **IVT 8×4 矩阵**:统计件就绪(`scripts/crrna_ivt_template.py --anova`),数据 data-pending——回补后与 §E 同源层闭环
3. **AF3/Chai 云端结果回填**:13 套输入已生成,跑完 zip 放 `data/af3_results/`(或 protenix_results/)用 collect 脚本回填
4. 答辩口径建议:分型 = 「同源原理统计(§E)+ 本体系计算实例(§A)」双层,功能收益"待湿实验"如实标注;Sp8 四模型 FAIL 只限 Creutzburg 侧,别被问穿

## 6. 纪律提醒(本仓库铁律,换人接手前必读)

- **判据先登记后评估**:改 PASS/FAIL 阈值/口径,先改 `docs/preregistration.md` 注明日期再改代码
- **失败判据必须留在 README 头条**,不许埋在 JSON 或删披露
- **判读文字由数值按显式规则生成**,禁止手写结论
- **严禁行尾污染**:本次已还原 409 个纯 CRLF/LF 翻转假改动(WSL+Windows 混用易触发);改动文件前确认 git diff 不是"全删全加同内容"
- 结果自检:脚本产出 + 独立审计(`*_audit.py`)+ 契约测试,三件套齐了才算完成
