# GuideForge 判据预登记（PASS/FAIL criteria registry）

- **登记日期**: 2026-09-04
- **性质**: 本文件是**补登记（retroactive consolidation）**，不是这些判据的首次成文——
  各判据此前散落在评估脚本内（多数与评估代码同文件、同提交），首次成文时间以 git
  历史为准。本文件把它们集中一处，并如实标注每条判据的来源性质。
- **生效规则（自登记之日起）**: 任何 PASS/FAIL 判据的**新增、改值、改口径**，必须先
  修改本文件并注明日期，再改评估代码、再跑评估运行。判据与评估运行同提交落地即
  视为违反本规则。失败的预登记判据必须出现在 README 头条结论中，不得只留在 JSON 内。
- **评审背景**: round-2 评审共同关切 #2（alfa #4/#6, bravo #1, charlie #7）指出
  仓库自称"预登记/先定后跑"的多个阈值实际由被评估数据导出，或普遍触发后被事后
  重述。本文件为该关切的整改物。

## 来源性质的诚实标注定义

- **真预登记**: 判据在对应评估运行之前已成文（有独立于评估代码的、带日期的载体），
  且数值不来自被评估数据。
- **项目设定常数（同文件定义）**: 判据是人为选定的常数，不来自被评估数据，但写在
  评估脚本内、与评估同提交，无独立带日期载体。自称"先定后跑"名不副实；本文件的
  登记使其此后具备预登记效力，但历史运行不适用"预登记"表述。
- **事后从数据导出**: 判据数值是被评估数据（或其子集）的函数。其中若使用了
  留出/测试部分的数据，另标注"**泄漏**"。

---

## A. 上下文分型 go/no-go 判据

- **判据原文**（`scripts/crrna_context_typing.py:188-190`）:
  > `go_no_go = 阳性(型间 TOP 差异显著): 各型并集不同且公共集小`
  > `if len(common) <= args.topk // 2 and len(per_type_sets) > 1 else 阴性…`
  即：各型代表重跑管线后，全型公共 TOP DR 子集大小 ≤ topk/2（默认 topk=8，即 ≤4）
  且型数 >1 → 阳性，否则阴性。k 按轮廓系数在 k=2..6 内自动选型（`crrna_context_typing.py:121-129`）。
- **当前实现位置**: `scripts/crrna_context_typing.py:188-190`（判定），
  `data/context_typing/typing.clusters.json` 字段 `go_no_go` / `top_common_to_all_types`。
- **来源性质**: **项目设定常数（同文件定义）**。阈值 topk//2 为人为常数，非数据导出；
  但判据写在评估脚本内、与评估同提交，此前无独立预登记载体。
- **最新结果**（2026-09-03 运行）: 公共集 = {A1C, A1G, A1U} 共 3 条 ≤ 4 → **阳性**。
  如实披露: 判据仅以 1 条变体的余量通过（公共集若为 5 条即翻转为阴性），且公共集
  3 条全为 position-1 惰性点突变；k=3 对 k=2 的轮廓系数优势仅 0.379 vs 0.373
  （差 0.006）；型 0/2（77/86 spacer）TOP 并集两两 Jaccard 高达 1.00/0.78，"各型最优
  位点图谱不同"主要由 junction 配对驱动的 n=9 小簇承载。该阳性结论为**弱证据阳性**。
- **下游消费点登记（2026-09-06，策划案 §5.3 末段/表5 风险1 对齐整改）**: 本判据
  自该日起被智能体直接消费为**阴性退化触发**——`scripts/crrna_design_agent.py`
  的 `interaction_assessment()` 每次运行从 `--clusters` 产物（各型代表
  `typing.t*_*.top.json`，与判据同源的 topk=8 切片）重算公共集并与归档
  `top_common_to_all_types` 交叉核对；判据未通过（阴性）时智能体输出从分型推荐
  **退化为通用型单骨架推荐**（`universal_scaffold()`：WT 0 基线 + 各型代表骨架中
  管线分全场最优，并列 WT 优先），输出 JSON 的 `degradation` 块如实携带模式、
  判据数值、交互弱量化证据与"科学结论依然完整可交付"交付口径；`--degrade on/off`
  为强制演示/覆盖旋钮，强制状态以 `forced` 字段如实标注。Web 演示页
  （`scripts/crrna_agent_webapp.py`）启动时同源评估一次并在页面如实展示退化状态。
  判据本体未改（阈值 topk//2、型数>1 不变），本次仅登记新消费点；契约测试
  `tests/test_degradation_mode.py` 锁定边界（公共集 4/5）、真实归档判型、
  触发/不触发两场景端到端行为与强制路径标注。

## B. 系综采样检验 R1–R4 规则

- **判据原文**（`scripts/crrna_ensemble_check.py:13-17, 37-40, 173-182`）:
  > R1 |ΔΔG_mfe − ΔΔG_ens| > 1.0  (MFE 点估计偏移过大)
  > R2 std(ΔΔG_sample) > |ΔΔG_ens| (采样噪声超过效应量)
  > R3 max−min(ΔΔG_sample) > 4.0  (跨构象跳变过大)
  > R4 茎完整构象占比在 20%~80% 之间 (构象二态)
- **当前实现位置**: `scripts/crrna_ensemble_check.py:37-40`（常量表 RULES）、
  `:173-182`（触发逻辑）；输出 `data/ensemble_check.{pdbdr.v2,zengdr.v6}.json`。
- **来源性质**: **项目设定常数（同文件定义）**；脚本自述"判不稳阈值为项目设定，
  非文献出处"。此前无独立预登记载体。
- **统计构造缺陷（如实标注，alfa round-2 #4）**: R2/R3 作用的统计量
  `ddG_samp = e_v − e_w` 把**两组相互独立的 Boltzmann 抽样按采样序号配对**求差，
  其 std 按构造 ≈ √(sd_v² + sd_w²)（本库实测 ≈ √2×0.9~1.0 ≈ 1.3 kcal/mol，与观测
  ddG_sample_std 1.3~1.4 一致）——R2/R3 是在普通系综涨落尺度上武装的噪声探测器，
  对 |ΔΔG_ens| ≲ 1.3 kcal 的候选按构造必然触发，**无法检出它想检的病理**。修复方向
  （未实施）: 改用匹配热力学量（如窗口均值能量差的 bootstrap），或用 WT 分裂半
  （WT-vs-WT split-half）标定零触发率后再定阈。
- **判据重设计（2026-09-04 round-3 补充，登记先于评估运行）**: 旧 R2/R3 退役（统计构造
  病态，见上；旧数值保留于输出 JSON `deprecated_ill_posed_R2R3` 仅供溯源，不再参与判定），
  替换为良态统计量——
  > R2′ 序翻转检验：仅对声称有实效的候选（|ΔΔG_ens| ≥ 1.0 kcal/mol，项目设定常数）判定；
  >   P_contra = P（逐构象能量序与窗口均值序相反），由两枚举窗口 Boltzmann 权重卷积精确计算
  >   （无采样序号配对、无抽样噪声）; P_contra > 0.2（项目设定常数）→ 触发。
  >   |ΔΔG_ens| < 1.0 的近中性候选不判定（其"无实效"即主张本身），P_contra 随行披露。
  > R3′ 景观粗糙度：spread_ratio = sd_ens(E_var)/sd_ens(E_WT)（枚举窗口内 Boltzmann 加权
  >   精确值，非 100 采样估计）; ratio > 2.0（项目设定常数）→ 触发。零分布标定：WT 枚举
  >   集随机分裂半 200 次，ratio 零分布 p95/max/超阈经验率随输出 `r3_null_calibration`
  >   报告（阈值假阳性率须 ≤1% 方可采信）。
  R1/R4 不变；结论强度由 R1+R2′+R3′+R4 共同决定。
- **最新结果**（2026-09-04 重设计判据评估，登记先于运行）: **重设计判据下两口径
  `conclusion_strength` 均为 `passed_all_rules`**——R1 触发 0/24（|shift|≤0.03）；
  R2′ 序翻转：6 条实效候选（|ΔΔG_ens|≥1.0，pdbdr 4 + zengdr 2）P_contra 最大
  0.13（阈 0.2），触发 0/24；近中性候选 18 条按登记不判定（P_contra 0.22~0.78
  逐候选披露，属普通热涨落重叠）；R3′ 景观粗糙度：展宽比最大 1.14（阈 2.0），
  触发 0/24，WT 分裂半零分布超阈经验率 pdbdr 2/200=1.0%、zengdr 1/200=0.5%
  （≤1% 采信线内）；R4 触发 0/24（茎完整率改枚举窗口精确值，范围 0.83~0.87，
  消除原 100 采样估计在 0.8 判界附近的噪声）。**"打分输入端在系综采样下稳定"
  主张在重设计判据下成立。** 历史记录（如实保留）：旧 R2/R3（病态构造）曾触发
  19/24、24/24（zengdr 10/12、pdbdr 9/12），当时对外只报 R1 腿的"主张成立"，
  属不实头条；round-3 先降级为 `downgraded_R1_leg_only`，本次判据重设计并登记后
  方升级为通过——旧数值留档 JSON `deprecated_ill_posed_R2R3`（构造值
  √(sd_v²+sd_w²) 与观测 ddG_sample_std 逐候选最大偏差 0.08/0.09 kcal，复现一致）。

## C. 选型分类器可用性阈值（fig1f 留出验证）

- **判据原文**（修复前 `scripts/crrna_train_selector.py:310`）:
  > `thr = (y.max() − y.min()) / 3  # 预登记阈值: 全体 y 值域 1/3`
  配合脚本内判定约定: Spearman>0 且 MAE < 阈值 → fig1f 转录配对可用于绝对水平。
- **当前实现位置**: 修复后 `scripts/crrna_train_selector.py` fig1f 留出段；输出
  `data/selector_model.json` 字段 `fig1f_holdout_check.mae_threshold_trainfold`。
- **来源性质**: 修复前为**事后从数据导出 + 泄漏**——`y` 为全量 16 点标签向量，
  含 9 条留出测试标签（y.max()=S20=0.928 本身是测试点），阈值 0.1617 是测试集的
  函数，"预登记"名不副实。**2026-09-04 修复**: 改用训练折（7 条工具箱）y 值域的
  1/3（训练折范围 0.443–0.583，阈值 = **0.0467**），字段改名 `mae_threshold_trainfold`。修复后
  仍属**事后从数据导出（训练折）**的相对阈——不泄漏测试标签，但也不是文献论证的
  外部绝对标准；此处如实标注，不自称预登记。真正有预登记效力的做法（未来）: 在见到
  fig1f 数据前按文献/测量精度论证一个绝对 MAE 阈并登记于本文件。
- **最新结果**: MAE 0.217，新旧两口径阈值（0.1617 / 0.0467）下均**失败**，判定的
  方向与后果（fig1f 配对仅用于秩级体检、绝对水平不采用）不变；失败的 MAE 判据
  必须保留在 README 头条表述中，不得改写为"秩级通过"遮蔽。同日 tie-aware 口径
  重算秩一致性: Spearman +0.25 → **+0.388**（bootstrap CI95 [0.00, +0.864]，
  10000 次重抽样中 1033 次因并列秩零方差剔除）；MAE 0.2167 不变。

## D. 阴性对照方向性检验 V1–V3 + 方向性判据

- **判据原文**（`scripts/crrna_negative_control.py:14-18, 134-141`）:
  > V1 工程茎破坏 4 条全部被硬过滤拦截(passed=False);
  > V2 每条阴性对照分数低于通过库中位数;
  > V3 ≥80% 阴性对照分数低于通过库第 10 百分位;
  > 方向性: 阴性对照 bp_dist 均值 > TOP-12 均值, p_fold 均值 < TOP-12 均值。
- **当前实现位置**: `scripts/crrna_negative_control.py:134-141`；输出
  `data/negative_control.{tp53_r248q_pdbdr.v2,tp53_r248q_zengdr.v6}.json` 字段 `verdict`。
- **来源性质**: **项目设定常数（同文件定义）**。80%、p10、中位数均为人为常数，
  非数据导出；但写在评估脚本内、与评估同提交，此前无独立预登记载体。
- **最新结果**（2026-09-03 运行，每口径 19 条阴性对照）:
  - pdbdr.v2 口径: V1 **PASS**, V2 **PASS**, **V3 FAIL**, 方向性 **PASS**;
  - zengdr.v6 口径: V1–V3 全 **PASS**, 方向性 **PASS**。
  pdbdr 口径的 **V3 失败是预登记判据的失败**，须出现在 README 头条（此前 README
  无该模块的结果段落，失败判据只存在于 JSON 内）。

---

## 附：本轮已定位、尚未迁移登记的其他同文件判据

以下判据同为"评估脚本内定义的项目设定常数"，charlie round-2 #7 已点名，本文件
先行登记在案（同上规则约束其未来变更），其表述/数值的文献化论证留待后续轮次:

| 判据 | 位置 | 内容 |
|---|---|---|
| Teng 4n96 不误伤阈 | `scripts/crrna_teng4n96_validate.py:117` | 相对分数在 zengdr.v2 passed 分布中百分位 ≥ 25% → PASS |
| 正向项区分力闸门 | `scripts/crrna_scaffold_design.py:961-966` | 非零比例 < 5% 即声明该指标在本库无区分力 |
| cross_pp 混杂归因 R1 | `scripts/crrna_cross_decompose.py:12,130` | \|Spearman(z_cross_coreDR)\| < 0.3 或 LOO 变号 → raw 信号归因成分混杂 |

## E. 同源跨上下文 DR 效应交互检验(分型假说的原理层统计判定)

- **登记日期**: 2026-09-08 声称/2026-09-09 实际入库(整改: 2026-09-09 审计认定)。
  **诚实标注(2026-09-09 外部审计后整改)**: 本节判据文本、评估脚本、评估结果与
  契约测试实际同落一个提交(e03251d, 09-09 01:31), 无 09-08 的独立带日期载体
  (本地未推送 commit/tag/时间戳)可证登记先行——**按本文件生效规则, 该情形
  即为违规**, 原"登记先于评估运行"表述不成立, 现予撤回并改标来源性质。
- **背景**: 湿实验矩阵(策划案 8×4 IVT 动力学矩阵)未执行,v4 湿实验仅剩细胞杀伤
  2 靶×2 条,不足以喂养"骨架×靶标交互"的统计判定。本判据把分型假说(骨架/DR
  效应的最优解随靶标上下文变化)的**原理层**判定迁移到同源(Cas12a 家族)大库:
  只回答"跨上下文 DR 效应交互是否为该家族普遍现象",**不替代 Cas12a2 本体系验证**
  (后者维持"功能收益待湿实验"口径)。与 §A(本体系计算分型判据)并列,构成
  "同源原理统计 + 本体系计算实例"双层证据;同源层为 AsCas12a/LbCas12a,迁移边界
  按 README「Cas12a→Cas12a2 同家族短迁移(DR 均含保守 5' 假结,非跨体系假设)」引用,
  并如实标注结构域差异(AsCas12a/LbCas12a 假结承载 vs Cas12a2 成熟 crRNA 无假结)。
- **判据原文**(`scripts/crrna_homolog_context_interaction.py`,2026-09-08):
  - **E1(主判据,DeWeirdt 2020 跨上下文一致性)**: 同一批 alt-DR 变体
    (cls=test, n=35682)在 pRDA_127(MCL1 guide)/pRDA_128(BCL2L1 guide)
    两个 spacer 上下文下的负筛 LFC 秩相关 Spearman ρ_cross(两方向同为
    "低=活性强",无需翻号;tie-aware midranks)。
    - **PASS(交互显著)**: ρ_cross < 0.4 且 bootstrap CI95 上界 < 0.5;
    - **FAIL(高度一致,支持通用型最优)**: ρ_cross ≥ 0.7;
    - 0.4 ≤ ρ_cross < 0.7: 不判(中等一致,如实报告)。
    阈值 0.4/0.7 为**项目设定常数**(非文献导出)。n_mut 分层、intend-6T/random
    对照类跨方向 ρ 随行披露(噪声/设计对照,不参与判定)。
  - **E2(佐证,区位分层)**: 变体按 DR 突变区位(茎配对区为主/环单链区为主/混合,
    区位=WT DR 的 ViennaRNA MFE 伪结外折叠近似)分层计算 ρ_cross,区位间差异
    Fisher z 双侧检验;不预设方向,如实报告(机制性描述,不参与 PASS/FAIL)。
  - **E3(佐证,Tian 2025 跨 crRNA;2026-09-08 首次评估后细化为 E3a/E3b 双层,
    本登记为细化后口径)**: 12 个 DR 5' 端(RRS)单点突变 × 8 条 crRNA(spacer 上下文)
    的 rrs_rel(mut/WT 活性比)。
    - **E3a 位置梯度(通用层)**: 全 12 突变 × 8 crRNA 的 Kendall W。预期高值
      (实测 W=0.769)由位置梯度驱动——RRS 3/4 位假结核心近乎全灭活、位置 1 最
      耐受, "哪一位敏感"跨 crRNA 稳定(通用层, 与 Cas12a2 本体系 §A 公共集
      A1C/A1G/A1U 同为位置 1 惰性跨体系呼应)。
    - **E3b 碱基级偏好(交互层)**: 位置 1(耐受位)3 突变 × 8 crRNA 的 Kendall W:
      **W < 0.3 → 交互佐证(E1 同向)**——同一耐受位点上"哪个碱基最优"随 spacer
      上下文翻转(实测 W=0.0156, 最优碱基分布 1C×4/1G×2/1U×2);
      W ≥ 0.7 → 碱基偏好跨上下文稳定(通用佐证); 中间如实报告。
    - 佐证不参与 E1 的 PASS/FAIL; E3a/E3b 方向相反不是矛盾——位置级敏感图谱
      (通用)与位点内碱基最优(上下文特异)是两个层次, 后者正是分型假说的
      同源实例。
- **来源性质**: **项目设定常数(同文件定义, 与评估同提交)**——E1/E2 的阈值
  (0.4/0.7)为人为常数、非数据导出, 但载体与评估同提交, 历史运行不适用
  "预登记"表述(与 §A 同级口径); E3b 的 W<0.3 阈值为**首次评估后细化**
  (见过数据 W=0.0156 后定型, 属事后判据, 单独标注)。本登记使其此后具备
  预登记效力, 此后变更须先改本文件。
- **下游消费**: E1 PASS → "分型假说获同源跨上下文大样本统计支持"进入 README
  分型证据链(与 §A 弱证据阳性 + 置换 P=0.036 并列);E1 FAIL → 结论如实写
  "同源大库未支持跨上下文交互(支持通用型最优)",进 README 头条,本体系维持
  §A 计算口径与"待 IVT 矩阵"标注——两种结果均完整可交付,不改变 §A 判据本体。
- **已知局限(随行披露)**: DeWeirdt 仅 2 个 spacer 上下文(回答"效应是否普遍一致"
  的最大样本检验,不是"型"的统计);负筛 LFC 非动力学;Tian 12 变体集中于
  RRS 假结区,动态范围受限,迁移到 Cas12a2 时按 README 边界声明引用。

## A2. 上下文分型判据强化（2026-09-10 登记；登记先于本轮重评估）

- **动机**: §A 弱证据阳性的三个触发项——判据随 k 翻动（k=2 不通过）；
  公共集全为 position-1 惰性突变；仅以 1 条余量通过。本节不替换 §A
  判据本体（智能体退化触发仍消费 §A），而是登记强化口径，服务分型
  证据强度评级。
- **A2a k 选型预登记化**: k 固定为 3（先验依据：策划案 V3 预期 3–4 型；
  §A 运行落点为 t0/t1/t2 三型）。轮廓系数（含多方法谱）仅作诊断随行
  报告，不再参与选型——「判据随 k 翻动」触发项自此关闭。v47 扩库
  （spacer 上下文从 n=86 扩至数百）后 k=3 不变；若扩库后轮廓谱强烈
  支持其他 k，按变更流程先改本文件再重评估。
- **A2b 主判据升级为置换检验**: 主判据从「公共集条数 ≤ topk/2」升级为
  置换检验——9 个型代表 TOP-8 列表随机分组零分布下 P（公共集 ≤ 观测值）
  < 0.05 → 阳性（零分布构造沿用 `crrna_typing_robustness.py` 补救①的
  精确枚举，阈值 0.05 为项目设定常数）。条数判据降级为描述统计随行报告。
- **A2c 惰性位点剔除版（主结论口径）**: TOP 列表先剔除全部含 position-1
  突变的变体（惰性位点；同源佐证 Tian E3a 位置 1 跨 crRNA 最耐受），按秩
  重排取 TOP-8，再做 A2b 置换检验。含/不含 pos-1 两层并列报告，
  **分型证据评级以 A2c 为准**。
- **来源性质**: 项目设定常数（阈值、k 固定值均为人为常数，非数据导出）；
  本登记先于 §A2 口径的首次运行。

## F. v4 细胞实验判据（8 数据点版；2026-09-10 登记，登记先于数据）

- **设计**: 2 骨架（WT / A1G+U3A+A8G+U15C 综合取向代表）× 2 靶
  （TP53-R248Q/HCT116 内源杂合、TP53-R273H/SW480 内源）× 突变/WT TP53
  细胞背景，n=3 生物学重复，CellTiter-Glo 杀伤曲线（剂量梯度 × 时间点，
  主读数 = 固定剂量 72h 活力或曲线下 AUC，分析方法先于数据固定）。
- **F1 等位选择性轴（主判据）**: 每条 crRNA 的选择性指数 SI =（突变株
  活力下降）−（WT 株活力下降）；SI > 0 且组间检验（双侧 t，正态性检验
  失败时改 Mann-Whitney）p < 0.05 → 等位选择性成立。4 条 crRNA 分别判定。
- **F2 骨架效应轴**: 同靶同突变株内，综合代表 − WT 的杀伤差 > 0 且
  p < 0.05 → 骨架优化的细胞水平证据成立（每靶分别判定）。
- **F3 交互轴（分型最小检验，探索性）**: 2×2（骨架×靶标）交互项——
  增益（综合代表 − WT）在 R248Q 与 R273H 间的差；双因素 ANOVA 交互项
  p < 0.05 → 骨架效应随靶变化（细胞级分型信号）；不显著 → 「骨架效应
  跨靶一致（通用型）」。n=3 功效低，如实标注探索性，不与 §A/§E 的
  PASS/FAIL 混用。
- **来源性质**: 项目设定常数；登记先于任何湿实验数据。

## A3. 特征增强版分型评估（2026-09-10 登记；登记先于运行）

- **动机**: v47 扩库在 §A2 口径下阴性（A2c P=0.057 擦线），归因为 5 维
  伪结外全长特征分辨率不足（DeWeirdt 大库实测该类特征仅携带 ~3% 秩方差）。
  本节登记特征增强版评估，检验「换更高分辨率的互作特征后分型信号是否恢复」。
- **A3a 特征向量扩充**（在 §A 原 5 维基础上增加，全部伪结外、同一 DR）:
  seed5_up（spacer 5' 端 5nt 游离度, 系综配分口径）; cross_stem / cross_loop
  （spacer-DR 交叉配对中涉及 DR 茎位 / loop 位的对数, 茎/loop 分区用
  flanked-by-paired 口径）; junction_maxrun（跨 DR-spacer 交界的最长连续
  配对数）; coupling_dG = MFE(DR+spacer) − MFE(DR) − MFE(spacer)（耦合力学项）。
- **A3b 评估**: 同一扩库队列（文献 86 + v47 新增 226, n=312,
  `data/context_typing/spacers_v47_expansion.txt`），k=3 预登记固定，
  判据同 §A2b/A2c（置换检验 P<0.05，含/去 position-1 两层并列，
  去 pos-1 为主结论口径）。
- **A3c 判读规则（先于数值固定）**: A3 阳性且 A2(v47) 阴性 → 支持
  「特征分辨率不足」归因，分型计算层证据升级；A3 亦阴性 → 计算层不支持
  分型，主张转交 §E 同源层（E1 已 PASS）与 §F 湿实验交互轴裁决——
  两种结果均完整可交付，不改动 §A/§A2 判据本体与既有结论。
- **来源性质**: 项目设定常数（特征定义、阈值均为人为常数）；
  本登记先于 A3 口径首次运行。

## G. Round-4 负面结果整改判据批次（2026-09-11 登记；登记先于评估）

本批针对 2026-09-10 盘点的负面结果清单 #1/#3/#4/#5/#6/#7/#8。
所有判读文字由数值按下述显式规则生成，不手写结论方向。

### G1. cross_pp 混杂归因强化检验（对应 #8）

- **现状**: raw cross_pp × 活性 ρ=+0.61（mature18 口径 raw_coreDR +0.55）已按
  「成分混杂」处理（单碱基组成匹配 z 后 +0.17/+0.21，GC 偏相关后 +0.08/+0.48，
  `scripts/crrna_cross_decompose.py`）。遗留缺口: 只单控 GC，未控混杂的另一条腿
  「spacer 5' A 富集」，且无显著性检验。
- **G1a 双协变量偏 Spearman**: 秩残差同时控制 spacer GC 含量与 A 含量
  （对两协变量的秩做多元线性回归取残差），mature18/pre25 × raw/z 全部格点报告。
- **G1b GC 分层置换**: 按 spacer GC 中位数分两层，层内置换活性标签
  （保持层边际分布），n_perm=20000，报分层置换 p（raw_coreDR mature18 为主口径）。
- **G1c 判读规则（先于数值）**: 双控后 |ρ|<0.3 且分层置换 p≥0.05 → 「成分混杂」
  归因成立并强化，README 表述不变、补双控数值；双控 |ρ|≥0.3 且分层 p<0.05 →
  归因不完整，README 修订为「残留信号方向为正、强度中等，最终裁决移交 §F 湿数据」。
  两种结果均如实入库。

### G2. 选型器排序稳定性量化（对应 #6 / 负面清单 #9）

- **块内容**: 对池化训练集（load_pooled）bootstrap 行重抽样 B=500 重训决策树
  （超参与池化模型一致, max_leaf=6, min_leaf=2, seed 递增固定），对 8 员族计算
  每条的 TOP1 入选频率与中位秩的 95% CI；Borda 计数聚合跨模型（决策树 + Ridge
  第二口径）排名。
- **G2 判读规则（先于数值）**: TOP1 bootstrap 入选频率 ≥50% → 「首位稳定」；
  <50% → 明示「首位弱稳定」并列报告频率；Borda 首位与单模型首位不一致 →
  以 Borda 为报告口径并标注分歧。
- **来源性质**: 披露性判据，不改「排序权威=管线打分」定位。

### G3. 位点敏感性×保守性合并检验（对应 #7 前半）

- **现状**: 三口径（score-adj / mean-score / tolerance）ρ=−0.335/−0.392/−0.426，
  置换 p=0.16/0.10/0.08，n=19 功效低、方向一致为负（`crrna_dmytrenko_validate.py`
  检验B）。
- **G3a 置换校准 Fisher 合并**: 统计量 X²=−2Σln(p_i)，零分布由**同步置换**生成
  （同一置换同时重算三口径 ρ 与各自置换 p），n_perm=20000——正确处理三口径间
  相关性，不假设独立。
- **G3b 判读规则（先于数值）**: 合并 p<0.05 且三口径方向一致为负 →
  「与天然进化数据一致」升级为合并检验口径显著；否则维持「方向一致、
  单口径不显著」表述。

### G4. DeWeirdt 大库位点敏感性×保守性（对应 #7 后半）

- **数据**: `data/raw/deweirdt2020_dr_scan.json`（35,883 条 AsCas12a alt-DR，
  LFC 双方向, 不入 git）取 n_mut==1 子集，按突变位置聚合位点敏感性
  = mean(−LFC127) / mean(−LFC128) 两方向（−LFC 大 = 该位突变失活强 = 位点敏感）;
  保守性 = `data/dr_conservation.json` 比对列 identity 经 AsCas12a 比对行映射到
  逐位坐标（三档分区口径并列报告）。
- **G4a 检验**: Spearman（tie-aware）+ 置换 p（n=20000）；主口径 =
  mean(−LFC) × 连续 identity。
- **G4b 判读规则（先于数值）**: 两方向均 ρ<0 且 p<0.05 → 大库独立证据支持
  「敏感位点富集于保守位点」（打分逻辑与天然进化一致的第二条独立证据链）；
  方向分裂或均不显著 → 如实报告，不改动 G3 结论。

### G5. Creutzburg 论文口径对齐重测（对应 #1）

- **对齐点**: 论文自报正相关的口径 = pre-crRNA 上下文（5' 残留 repeat + DR +
  spacer）+ 茎定义含手工假结 P1 配对（U(-10):A(-18) 反 Hoogsteen，FnCas12a/
  AsCas12a 同源位坐标，结构文献口径）。可计算近似: hc_add_bp 强制 P1 配对的
  约束配分下，5 对经典茎 WC 配对（-6..-2 × -11..-15）的联合完整概率
  stem_prob|P1；另报 ΔG_pk = dG(P1 强制) − dG(无约束) 作假结配对热力学代价代理。
- **G5a 判读规则（先于数值）**: stem_prob|P1（pre25 口径）× 活性 ρ>0 且置换
  p<0.05 → 「跨体系复现失败」归因于口径不齐获支持，README 补「论文口径对齐后
  符号复现」；ρ≤0 或不显著 → 维持现表述并追加该口径亦阴性的记录。n=14
  小样本限制照写。

### G6. RRS 位置感知模型（对应 #5 / 负面清单 #7）

- **先例**: CRISPRpred（Liu 2020）位置特异性特征提升预测。
- **模型**: Tian 2025 RRS 96 条，特征 = 突变位置 one-hot(4) ⊗ 突变碱基
  one-hot(3) + 5 维结构特征，Ridge(alpha=1.0)；验证 = 留一 crRNA 交叉验证
  （LOCO, 8 折, 组间独立）；消融 = 仅位置特征 / 仅结构特征 两对照。
- **G6a 判读规则（先于数值）**: LOCO Spearman ≥ +0.3 → RRS 区候选从
  「按位置规则排除」升级为「位置感知模型打分（仅 Tian 同族 RRS 区、
  仅相对排序）」；<+0.3 → 维持排除规则，数值入库。

### G7. 层级部分池化（对应 #3/#4 / 负面清单 #6、#8）

- **模型**: 数据集/终点随机截距 + 共享 5 特征斜率的线性层级模型（交替最小
  二乘: 给定截距拟合共享 Ridge，给定斜率按组均值更新截距，迭代至收敛，
  固定种子, 无外部依赖）；池化集 = 现 5 终点 + DeWeirdt 120 条 LFC 秩分层
  子样（带自身截距入池，与 2026-09-03 拒入实验同子样）。
- **评估**: LOEO 在层级口径下重算（留出终点整组，其截距收缩至 0）。
- **G7a 判读规则（先于数值）**: DeWeirdt 入池后其余 5 终点 LOEO 保持正值的
  终点数 ≥3（多数）→ 池化口径从「拒入」修订为「层级部分池化入池」；
  否则维持拒入并报告层级口径数值。cis 终点层级 LOEO 与现值 −0.692 并列报告:
  改善则更新，不改善则保留原值。层级模型仅作终点整合第二口径，与决策树
  池化版并存，分歧并列报告。

## 变更记录

- 2026-09-04: 首版登记（round-3 整改，评审共同关切 #2）。同日配套代码整改:
  选型器阈值去泄漏并改名 `mae_threshold_trainfold`；系综检验 JSON 增加全规则
  汇总、结论强度降级与 ill-posed 标注；全仓库手搓 Spearman 统一为
  scipy.stats.spearmanr（tie-aware midranks）。
- 2026-09-08: 新增 §E 同源跨上下文 DR 效应交互检验（分型假说原理层统计判定，
  登记先于评估运行；配套脚本 scripts/crrna_homolog_context_interaction.py）。
- 2026-09-10: 新增 §A2 分型判据强化（k=3 预登记固定 + 置换检验升主判据 +
  去 position-1 惰性位点版为主结论口径）与 §F v4 细胞实验判据（F1 等位选择性
  /F2 骨架效应/F3 交互轴）；两者登记均先于评估/数据（本提交即为登记载体）。
- 2026-09-10: 新增 §A3 特征增强版分型评估（10 维互作特征; 判读规则先于数值固定; 登记先于运行）。
- 2026-09-11: 新增 §G round-4 负面结果整改判据批次（G1 cross_pp 双控偏相关+GC 分层置换 /
  G2 选型器秩稳定量化 / G3 位点×保守置换校准合并 p / G4 DeWeirdt 大库位点敏感性 /
  G5 Creutzburg P1 约束口径对齐 / G6 RRS 位置感知模型 / G7 层级部分池化）；
  登记先于各评估运行（本提交即为登记载体）。
